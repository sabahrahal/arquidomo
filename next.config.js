/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "is1-ssl.mzstatic.com",
        "res.cloudinary.com"
      ],
    },
  };
  
  module.exports = nextConfig;
